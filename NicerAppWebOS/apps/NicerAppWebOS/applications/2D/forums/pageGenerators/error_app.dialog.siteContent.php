<?php 
    global $naWebOS;
    $naWebOS->init();
    $view = $naWebOS->view;
    echo 'Invalid parameters for nicerapp forum app :<br/><p><pre>$view='.json_encode($view,JSON_PRETTY_PRINT).'</pre></p>';
?>
